import React from "react";
import ReactDOM from "react-dom/client";
import StudioOpsERP from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StudioOpsERP />
  </React.StrictMode>
);
